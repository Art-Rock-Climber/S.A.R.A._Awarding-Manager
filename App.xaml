<Application x:Class="sara_coursework.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:sara_coursework"
             xmlns:helpers="clr-namespace:sara_coursework.Helpers"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <!-- Общий стиль для кнопок в приложении -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FFF2F4F7"/>
            <Setter Property="Foreground" Value="#FF344054"/>
            <Setter Property="BorderBrush" Value="#FFD0D5DD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter"
                                              Focusable="False"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#FFE4E7EC"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF98A2B3"/>
                                <Setter Property="Foreground" Value="#FF1D2939"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#FFD0D5DD"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF475467"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="border" Property="Background" Value="#FFF9FAFB"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFE4E7EC"/>
                                <Setter Property="Foreground" Value="#FF98A2B3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Общий стиль для DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="AlternatingRowBackground" Value="#FFF5F7FA"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFD0D5DD"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#FFE4E7EC"/>
            <Setter Property="VerticalGridLinesBrush" Value="#FFE4E7EC"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FFF9FAFB"/>
                        <Setter Property="Foreground" Value="#FF475467"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="BorderBrush" Value="#FFD0D5DD"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    </Style>
                </Setter.Value>
            </Setter>

            <!-- Включаем автонумерацию на всех таблицах -->
            <Setter Property="helpers:DataGridBehavior.ShowRowNumbers" Value="True"/>
            <!-- Настраиваем ширину колонки с номерами строк -->
            <Setter Property="RowHeaderWidth" Value="25"/>
            <!-- Стилизуем колонку с номерами под общий стиль приложения -->
            <Setter Property="RowHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRowHeader">
                        <Setter Property="Background" Value="#FFF9FAFB"/>
                        <Setter Property="Foreground" Value="#FF475467"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="5,0"/>
                        <Setter Property="BorderBrush" Value="#FFD0D5DD"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
